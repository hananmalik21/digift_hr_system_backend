import { REC_CANDIDATE_CONVERT_EMPLOYEE } from './recCandidateConversionConstants.js';

/** FNDSEC function codes for candidate-to-employee conversion (enforce when REC_ENFORCE_PERMISSIONS=true). */
export const CANDIDATE_CONVERSION_PERMISSIONS = {
  validate: REC_CANDIDATE_CONVERT_EMPLOYEE,
  convert: REC_CANDIDATE_CONVERT_EMPLOYEE
};
