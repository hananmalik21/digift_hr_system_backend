/** Re-export shared Oracle CLOB bind helpers (canonical: utils/oracleClobBinds.js). */
export {
  componentsJsonClobBind,
  textClobBind,
  nullableTextClobBind,
  readClobOut,
  parseJsonClobOut
} from '../../../utils/oracleClobBinds.js';
